

BF_TS_2digit

make the digits to 3 (more prices)



from BF_TS_2digit_neigdis import BF_TS

make the data structure from set to list, allow repeat element in a list

chan is in (10000,1024)
bf_train = bf_ts.only_store_neibour(chan)  # the result it a list and hard to convert to np array
only convert to list with neighbour (1000,1024*11), one value have 10 neighbour



